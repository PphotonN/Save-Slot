# Public web configuration. This value is bundled into the web application.
VITE_SAVE_SLOT_API_URL=http://localhost:8787

# Save Slot uses no-key public catalogue providers by default.
# Optional credential-based providers are not required for normal use.
