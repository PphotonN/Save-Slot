# Copy to apps/api/.dev.vars for local Worker development.
# Never commit the real .dev.vars file.
# The default Save Slot catalogue uses only no-key public sources.
# Wildcard CORS is intentional for public GET catalogue data and supports
# browsers plus Capacitor https://localhost.

ALLOWED_ORIGIN=*
